<%= form_for @criteria_type, :html => { :class => 'form-horizontal' }, :validate => true  do |f| %>
  <div class="field">
    <%= f.label :"Nombre del tipo de criterio" %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
