<h1>Lista de Tipos de Actividad</h1>

<table class='table table' >
  <tr>
    <th>ID</th>
    <th>Nombre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @activity_types.each do |activity_type| %>
  <tr>
    <td><%= activity_type.id %></td>
    <td><%= activity_type.name %></td>
    <td><%= link_to 'Eliminar', activity_type %></td>
    <td><%= link_to 'Editar', edit_activity_type_path(activity_type) %></td>
    <td><%= link_to 'Eliminar', activity_type, confirm: 'Esta seguro?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Tipo de Actividad', new_activity_type_path %>
